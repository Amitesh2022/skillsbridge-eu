import{setActivePinia,createPinia}from'pinia';import{beforeEach,describe,expect,it}from'vitest';import{useSkillsStore}from'./store';describe('skills store',()=>{beforeEach(()=>setActivePinia(createPinia()));it('saves and removes a pathway',()=>{const s=useSkillsStore();s.toggleSaved(1);expect(s.saved).toContain(1);s.toggleSaved(1);expect(s.saved).not.toContain(1)});it('creates a workforce cohort',()=>{const s=useSkillsStore();s.createCohort('AI leaders','Technology',20);expect(s.cohorts[0].seats).toBe(20)})})
